{
  "name": "@apps/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "dependencies": {
    "@langchain/langgraph": "^0.2.53",
    "@nudges/config": "workspace:*",
    "@nudges/domain": "workspace:*",
    "@nudges/knowledge-runtime": "workspace:*",
    "@nudges/persuasion-core": "workspace:*",
    "@nudges/provider-openai": "workspace:*",
    "@nudges/provider-sarvam": "workspace:*",
    "@nudges/safety-compliance": "workspace:*",
    "fastify": "^5.2.1"
  }
}
