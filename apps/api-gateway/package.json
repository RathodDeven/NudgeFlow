{
  "name": "@apps/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "dependencies": {
    "@nudges/config": "workspace:*",
    "@nudges/db": "workspace:*",
    "@nudges/domain": "workspace:*",
    "@nudges/knowledge-runtime": "workspace:*",
    "@nudges/observability": "workspace:*",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  }
}
